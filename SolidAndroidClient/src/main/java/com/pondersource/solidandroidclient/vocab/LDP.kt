package com.pondersource.solidandroidclient.vocab

import com.inrupt.client.vocabulary.LDP

object LDP {
    val namespace = LDP.getNamespace()
    val contains = LDP.contains
    val inbox = LDP.inbox
    val RDFSource = LDP.RDFSource
    val BasicContainer = LDP.BasicContainer
    val NonRDFSource = LDP.NonRDFSource
    val Resource = LDP.Resource
}